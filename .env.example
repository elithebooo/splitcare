# Stellar network SplitCare targets (testnet only)
VITE_STELLAR_NETWORK=testnet

# Soroban RPC endpoint used for contract calls and event sync
VITE_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Deployed SplitCare Soroban contract ID (filled after deployment, see scripts/deploy-contract.sh)
VITE_CONTRACT_ID=
